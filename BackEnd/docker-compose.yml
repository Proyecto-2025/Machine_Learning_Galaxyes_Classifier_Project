version: '3.9'

services:
  backend:
    build: .
    container_name: flask_backend
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - sqlite_data:/app/instance
    environment:
      FLASK_ENV: development
    command: ["python", "run.py"]

volumes:
  sqlite_data: